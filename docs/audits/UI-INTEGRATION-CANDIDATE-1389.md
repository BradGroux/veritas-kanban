# Combined UI integration candidate

This is a verification branch for #1389, not completion of the whole audit or a release. It combines the current heads of #1398 (full-CI request handling), #1412 (workflow timing), #1424 (Policy dialog focus), #1416/#1419 (Template authoring and priorities), and #1421/#1422 (responsive task shell and shared chat). Main already contains keyboard reorder and Template clearing.

Keep this integration PR draft and unmerged. Independently reviewed implementation PRs remain the delivery units. The full-CI label requests workspace tests, coverage, browser QA, security, and packaging contracts against one combined tree. Local integration checks concentrate on the changed rendered interactions; they do not substitute for the broad CI gate.

Scoped runtime and packaged macOS results are recorded below. Remaining overlay consumers and task-content convergence under #1383/#1385 are not silently accepted by a passing candidate. The installed application is unchanged. Final maintained screenshots/GIFs, installed-app acceptance, version bump, changelog, signed release, and release readback remain outstanding.

## Integration checkpoint

Local browser checks passed for repeated early-Escape/Cancel in Policy dialogs, Task Chat in both themes, and compact Workbench in both themes. Workbench needed a scoped Squad filter locator and an Escape-ordering correction; see the shared-chat audit. Activity/chat, Squad, layout-chrome, and overlay unit slices passed. The Template slice exposed obsolete tab-navigation expectations introduced during branch reconciliation; corrected expectations passed all 11 tests and were pushed to #1416/#1419. No passing unchanged slice was rerun for documentation changes.

Full CI on candidate d26f040b passed build, lint/typecheck, critical coverage, CodeQL, Gitleaks, Docker contract, load smoke, and unsigned macOS packaging. Workspace unit tests passed all 331 server files, then failed on the same four Template expectations now corrected. The production dependency audit timed out contacting the npm advisory endpoint; this is missing security evidence, not a clean audit. Browser QA was still running at this checkpoint. The earlier workflow branch run separately timed out in the SQLite device-session auth test; the combined server pass does not erase that diagnostic follow-up.

## Packaged macOS checkpoint: 80e5bc6d

`pnpm build` and `pnpm --filter @veritas-kanban/desktop package:mac:dir` passed. The local unsigned arm64 Electron application was launched from `desktop/release/mac-arm64/veritas-kanban.app`, with `app.isPackaged` asserted and an isolated temporary user-data directory. The packaged web index SHA-256 is `0de9c5eeae79083f40c74e2dcb99dc7dbf3e53131edf112b069289899d337499`. This is a candidate build still reporting version 6.1.6, not a signed release or replacement of the installed app.

Actual native-window checks passed in light and dark themes:

- Policy Edit/Test keyboard activation and immediate Escape, repeated twice, restore the exact opener at 1180×760 with 20px root text.
- Compact Board/Squad Chat retains drafts across channel switching. Filter-trigger Escape, nested Select Escape, parent-popover Escape, and sender-menu focus restoration pass. Resizing from 1700×900 to 1180×760 minimizes Workbench and sidebars.
- Task Chat restores non-header opener focus, preserves drafts and Results selection, handles nested confirmation Escape, and keeps Tab within the combined task/chat workspace. At the native minimum 1180×760, 16px text retains the sidecar and 20px text activates body takeover. Transcript/composer bounds and horizontal-overflow assertions pass. Mocked send verifies exact task ID, deterministic session ID, and message; no real provider message was sent.

The native minimum is 1180×760. Smaller browser viewport tests are not native coverage. Stable Task Chat captures were inspected at normal and enlarged text; transient Board/Squad captures are diagnostic artifacts, not maintained documentation media. Local harness and captures are under `/private/tmp/vk-native-ui-1389.8cJNtK/`; the successful task run used `NATIVE_SCOPE=task node verify.mjs` and exited zero. One harness assertion was corrected to check the retained Results heading instead of requiring a compact mode selector after chat closes and the normal navigation returns.

For this same source tree, CI run [33820019372](https://github.com/BradGroux/veritas-kanban/actions/runs/33820019372) passed workspace unit tests, critical coverage, build, lint/typecheck, changed tests, and scope selection. Security Audit failed because all three npm advisory-service requests timed out, so dependency security acceptance remains missing. Unsigned macOS, Windows, and Linux artifact jobs passed in [33820019250](https://github.com/BradGroux/veritas-kanban/actions/runs/33820019250). Browser QA [33820019232](https://github.com/BradGroux/veritas-kanban/actions/runs/33820019232) finished with 63 passing tests, one skipped, and one ambiguous containment-test locator: “Close Board Chat” also matched “Close Board Chat panel”. Requiring the exact toolbar accessible name corrected that locator; the complete focused containment-and-drag scenario passed locally afterward. The first local attempt used the alternate server port without overriding the helper's API base and failed before seeding; the corrected invocation sets both port variables and `API_BASE_URL` to the isolated endpoint.

These findings establish only the listed packaged interactions. Native Template authoring, remaining overlays/task content, final media, installed-app acceptance, and release work remain open. Keep the candidate draft and the audit goal open.

## Subsequent checkpoint: 169f7628 and Create Task

Candidate `169f7628` passed full CI [33821267360](https://github.com/BradGroux/veritas-kanban/actions/runs/33821267360), including workspace tests, coverage, build, lint/typecheck, and the production dependency audit that previously timed out. Browser QA and load smoke passed in [33821267373](https://github.com/BradGroux/veritas-kanban/actions/runs/33821267373); unsigned macOS, Linux, and Windows packaging passed in [33821267378](https://github.com/BradGroux/veritas-kanban/actions/runs/33821267378). Security Gates [33821267381](https://github.com/BradGroux/veritas-kanban/actions/runs/33821267381) and Docker contract [33821267384](https://github.com/BradGroux/veritas-kanban/actions/runs/33821267384) also passed. Those results precede the Create Task and task API changes below.

Native Template layout/dismissal checks passed in both themes at 1180×760 with 20px text, including all four dropdowns, equal field widths, validation focus, long-body scrolling, footer reachability, and nested discard Escape. Comparison exposed Create Task's legacy modal, split into #1426 / PR #1427. Native Template create/edit persistence remains open.

The Create Task browser cases passed in both themes at 900×480 with 20px text after explicitly adding the desktop bridge fixture; the existing four duplicate-detection unit tests passed. The native migration check then exposed an actual HTTP 400 for Critical priority. The separate #1428 API fix shares the priority contract across create/update, preserves Medium default, and rejects unknown values. Its two Critical regressions failed before the fix; all 56 task-route tests passed afterward.

The local candidate was rebuilt with the Create Task migration and task API fix. Native layout/focus/Escape/draft-retention and real Critical creation passed in both themes at 1180×760 with 20px text, including finding each synthetic task after reload in an isolated profile. One card locator needed the heading within the card rather than a truncated exact accessible name. A dark capture initially omitted the submit label; subsequent DOM inspection and recapture showed white, fully opaque text. These diagnostic images are not final media. See the source-slice audit documents for remaining template/blueprint-assisted creation and installed-app acceptance.

The installed application remains unchanged, and this is still an unsigned version-6.1.6 candidate. Final media, remaining overlay/task-content work, installed acceptance, version/changelog, signed release, and release readback remain outstanding. Keep this verification PR unmerged.

## Native persistence and parent-merge checkpoint

Candidate `a30e204b` passed full CI [33822684691](https://github.com/BradGroux/veritas-kanban/actions/runs/33822684691), browser QA [33822684689](https://github.com/BradGroux/veritas-kanban/actions/runs/33822684689), security gates [33822684687](https://github.com/BradGroux/veritas-kanban/actions/runs/33822684687), Docker contract [33822684688](https://github.com/BradGroux/veritas-kanban/actions/runs/33822684688), and all three unsigned desktop artifacts [33822684701](https://github.com/BradGroux/veritas-kanban/actions/runs/33822684701). Those results include Create Task and Critical task priority, but precede the blueprint API correction below.

Native Template Create/Edit persistence and template-assisted task creation passed in both themes. A saved Critical template retained edited Markdown through reload; Create Task inherited those values and created a real task. Workflow recovery #1412, Policy focus #1424, task shell #1421, and Template authoring #1416 were merged after their scoped acceptance checks. Parent audit issues remain open where they cover additional consumers.

The native blueprint scenario exposed #1430: POST task validation silently stripped subtasks and dependency IDs. Its four new regressions failed before correction; all 60 combined task-route tests passed afterward. The rebuilt unsigned macOS candidate passed the exact blueprint scenario in both themes at 1180×760, including a custom variable, subtask acceptance criteria, the generated prerequisite ID, and persistent values from the app's authenticated task-list response after reload. Only blueprint discovery was fixture-backed; task creation and storage used the real isolated-profile API. See #1431 and its audit document. The separate one-line Create Task helper correction now shows the parser's required custom-variable syntax and passed typecheck, lint, and both review axes; the native acceptance run preceded that wording-only correction.

Source branches were reconciled with their merged parents, preserving the already-tested chat implementation, shared priority import, and accepted parent audit evidence. This new candidate requires its own full CI result. The installed application and user data remain unchanged. Remaining chat/task-content acceptance, overlay consumers, final documentation media, the installed-app gate, and signed release are still open.

## Accepted chat and Board dialog checkpoint

Candidate `a028c84d` passed full CI [33824126199](https://github.com/BradGroux/veritas-kanban/actions/runs/33824126199), browser QA [33824126211](https://github.com/BradGroux/veritas-kanban/actions/runs/33824126211), security gates [33824126200](https://github.com/BradGroux/veritas-kanban/actions/runs/33824126200), Docker contract [33824126213](https://github.com/BradGroux/veritas-kanban/actions/runs/33824126213), and all three unsigned desktop artifacts [33824126251](https://github.com/BradGroux/veritas-kanban/actions/runs/33824126251). Template priority #1419, Create Task #1427, blueprint field preservation #1431, and shared chat #1422 are now merged. Shared chat's additional native tests preserved pending edits, task identity, section/scroll, older attempt and highlighted event through expanded and compact presentations in both themes. The production dependency audit on the final chat documentation commit timed out contacting npm, then passed on retry; it was not bypassed.

The local candidate `bda76b05` integrates those accepted source merges and Board dialog migration #1433. The only merge conflict was the shared-chat audit document; the accepted main-branch evidence was retained. The complete package command rebuilt the unsigned macOS app. In an isolated profile, all five Board dialog flows passed at 1700×900 with 16px text and 1180×760 with 20px text in both themes. Initial focus, exact opener restoration after Escape, full footer reachability, dialog bounds and horizontal containment passed. Captures of each dialog family were inspected. Unit callbacks are mocked; native task/settings/sprint fixtures and intercepted destructive requests avoid changes to real user data.

Inspection exposed a separate long-name archive-banner overflow, tracked in #1434. Dialog acceptance does not cover that background banner. Source #1433 has 17 focused unit checks, two browser scenarios spanning all five flows and both size classes, web typecheck/lint, and packaged native proof. Its final CI must still pass before merge. This checkpoint does not claim a new full CI result for `bda76b05`.

The installed application is unchanged. Remaining overlay/task-content work, #1434, whole-app conformance #1387/#1389, all final documentation images/GIFs #1388, version/changelog, signed release, and installed-app acceptance remain open. Keep this verification PR unmerged.

## Settings family checkpoint: 4f747e6f

Candidate `7e8b131e` passed browser QA and load smoke [33825802158](https://github.com/BradGroux/veritas-kanban/actions/runs/33825802158), security gates [33825802145](https://github.com/BradGroux/veritas-kanban/actions/runs/33825802145), Docker contract [33825802144](https://github.com/BradGroux/veritas-kanban/actions/runs/33825802144), and all three unsigned desktop artifacts [33825802157](https://github.com/BradGroux/veritas-kanban/actions/runs/33825802157). Full CI [33825802141](https://github.com/BradGroux/veritas-kanban/actions/runs/33825802141) passed its non-audit jobs, but the production dependency audit timed out at npm again on retry. This remains missing security evidence. Board #1433 and archive-banner #1435 final source commits also encountered repeated advisory-endpoint timeouts; neither is merged.

The rebuilt `07c5ae97` candidate passed Board popouts and long-name archive-banner containment in both themes at 1700×900/16px and 1180×760/20px. The subsequent `4f747e6f14c3e90549644dabf46aff29b658620f` candidate adds Settings #1437. Its unsigned arm64 packaged application passed every Settings child dialog at the same native sizes and themes, including visible initial focus, footer bounds, inert parent, Tab/Escape, and exact opener restoration. The five reported Tasks fields share their left edge, width, and height within 2px. The packaged `app.asar` SHA-256 is `57e855b0ab4d3a107ada9d9fe5dcdb6aea31e69c6cb73453cd27e195cf4b9aaf`.

Native screenshot review caught an offscreen initial focus target in cleanup review. Its fixed-footer Close action now receives focus, and strengthened browser plus rebuilt native checks require initial focus to be fully visible. No real reset, deletion, cleanup, save, or launch occurred during these fixture-backed layout checks. The owned native application was closed after acceptance. See the Settings audit for exact commands and artifact boundaries.

This checkpoint is not a full-CI pass for `4f747e6f`. Wrapped Shared Resources row actions remain a separate finding. Remaining overlay/task-content migrations, whole-app conformance, installed-app replacement, all maintained docs images/GIFs, version/changelog, and signed release are still unfinished. Keep this integration PR draft and unmerged.

## Settings risk actions and discovery checkpoint: 6044bd62

The Settings risk-action slice #1439 passed its packaged native checks on candidate `87ff80ad` in both themes and both native size classes. Full labels and accessible action names remain visible for shared-resource operations; no destructive action was executed. See the Settings risk-action audit for the source and packaged web asset evidence. It remains stacked on Settings #1437.

Discovery #1440 is now integrated and accepted on rebuilt candidate `6044bd62ea7265153a9e878c9e3b865605b161aa`. Search, Command Palette, and shortcut help passed the complete native flow at 1700×900/16px and 1180×760/20px in both themes with normal animations, including scrolling, fixed footer, menu Escape, route navigation, Search/Settings/Create handoffs, and exact opener restoration. A native selector was corrected to accept the Settings Board Only title; a capture taken during fade-in was replaced after waiting for full opacity. The actual packaged web index SHA-256 is `79f1d0fc46dfa115279fc5ce5a8978765e313809dbd205894ccd4b60fec3aa68`; see the discovery audit for the main JavaScript asset hash and exact harness. The owned application was closed after acceptance, and no real save, launch, or provider message was sent.

The preceding remote candidate `8f23036f` did not pass all gates. CI [33828047852](https://github.com/BradGroux/veritas-kanban/actions/runs/33828047852) failed only its production dependency audit after npm advisory requests timed out. QA [33828047823](https://github.com/BradGroux/veritas-kanban/actions/runs/33828047823) failed the two policy-action-layout cases because the closing Edit dialog still existed when the test used a generic dialog locator for Test; that test synchronization defect is tracked separately in #1441. Load smoke passed. Desktop packaging [33828047856](https://github.com/BradGroux/veritas-kanban/actions/runs/33828047856), security gates [33828047847](https://github.com/BradGroux/veritas-kanban/actions/runs/33828047847), and Docker contract [33828047834](https://github.com/BradGroux/veritas-kanban/actions/runs/33828047834) passed. The Settings source CI [33828002970](https://github.com/BradGroux/veritas-kanban/actions/runs/33828002970) also timed out at the advisory endpoint on its failed-job retry. Missing dependency security evidence is not waived.

This checkpoint records scoped acceptance, not a full-CI pass for the current tree. The installed application remains unchanged. Remaining overlay/task-content migrations, whole-app conformance, all maintained documentation images/GIFs, version/changelog, signed release, and installed-app acceptance remain unfinished. Keep this verification PR draft and unmerged.
